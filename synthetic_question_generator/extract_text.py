
text = {'keys': ['9e7f3d63-09f6-4a4a-97f7-c8a6c404a81d'], 'rationale': '<p style="text-align: left;">Choice C is correct. The area of a triangle can be calculated using the formula <math alttext="upper A equals one half b h"><mi>A</mi><mo>=</mo><mfrac><mn>1</mn><mn>2</mn></mfrac><mi>b</mi><mi>h</mi></math>, where <math alttext="upper A"><mi>A</mi>\n</math> is the area of the triangle, <math alttext="b"><mi>b</mi>\n</math> is the length of the base of the triangle, and <math alttext="h"><mi>h</mi>\n</math> is the height of the triangle. In the figure, triangle <math alttext="upper A upper B upper C"><mi>A</mi><mi>B</mi><mi>C</mi></math> is a right triangle with right angle <math alttext="upper B"><mi>B</mi>\n</math>. Therefore, the base of the triangle is <math alttext="upper A upper B"><mrow>\n\t<mi>A</mi>\n\t<mi>B</mi>\n</mrow>\n</math> and the height of the triangle is <math alttext="upper B upper C"><mrow>\n\t<mi>B</mi>\n\t<mi>C</mi>\n</mrow>\n</math>. It&rsquo;s given that <math alttext="upper A upper B equals 3 x plus 7"><mrow>\n\t<mrow>\n\t\t<mi>A</mi>\n\t\t<mi>B</mi>\n\t</mrow>\n\t<mo>=</mo>\n\t<mrow>\n\t\t<mrow>\n\t\t\t<mn>3</mn>\n\t\t\t<mi>x</mi>\n\t\t</mrow>\n\t\t<mo>+</mo>\n\t\t<mn>7</mn>\n\t</mrow>\n</mrow>\n</math> and <math alttext="upper B upper C equals 2 x plus 1"><mrow>\n\t<mrow>\n\t\t<mi>B</mi>\n\t\t<mi>C</mi>\n\t</mrow>\n\t<mo>=</mo>\n\t<mrow>\n\t\t<mrow>\n\t\t\t<mn>2</mn>\n\t\t\t<mi>x</mi>\n\t\t</mrow>\n\t\t<mo>+</mo>\n\t\t<mn>1</mn>\n\t</mrow>\n</mrow>\n</math>. Substituting <math alttext="3 x plus 7"><mrow>\n\t<mrow>\n\t\t<mn>3</mn>\n\t\t<mi>x</mi>\n\t</mrow>\n\t<mo>+</mo>\n\t<mn>7</mn>\n</mrow>\n</math> for <math alttext="upper A upper B"><mrow>\n\t<mi>A</mi>\n\t<mi>B</mi>\n</mrow>\n</math> and <math alttext="2 x plus 1"><mrow>\n\t<mrow>\n\t\t<mn>2</mn>\n\t\t<mi>x</mi>\n\t</mrow>\n\t<mo>+</mo>\n\t<mn>1</mn>\n</mrow>\n</math> for <math alttext="upper B upper C"><mrow>\n\t<mi>B</mi>\n\t<mi>C</mi>\n</mrow>\n</math> in the formula for the area of a triangle yields <math alttext="upper A equals one half left parenthesis 3 x plus 7 right parenthesis left parenthesis 2 x plus 1 right parenthesis"><mi>A</mi><mo>=</mo><mfrac><mn>1</mn><mn>2</mn></mfrac><mfenced><mrow><mn>3</mn><mi>x</mi><mo>+</mo><mn>7</mn></mrow></mfenced><mfenced><mrow><mn>2</mn><mi>x</mi><mo>+</mo><mn>1</mn></mrow></mfenced></math>, or <math alttext="upper A equals left parenthesis 3 x plus 7 right parenthesis left parenthesis x plus one half right parenthesis"><mi>A</mi><mo>=</mo><mfenced><mrow><mn>3</mn><mi>x</mi><mo>+</mo><mn>7</mn></mrow></mfenced><mfenced><mrow><mi>x</mi><mo>+</mo><mfrac><mn>1</mn><mn>2</mn></mfrac></mrow></mfenced></math>. It&rsquo;s given that the area of triangle <math alttext="upper A upper B upper C"><mi>A</mi><mi>B</mi><mi>C</mi></math> is <math alttext="54"><mn>54</mn>\n</math> square units. Substituting <math alttext="54"><mn>54</mn>\n</math> for <math alttext="upper A"><mi>A</mi>\n</math> in this equation yields <math alttext="54 equals left parenthesis 3 x plus 7 right parenthesis left parenthesis x plus one half right parenthesis"><mn>54</mn><mo>=</mo><mfenced><mrow><mn>3</mn><mi>x</mi><mo>+</mo><mn>7</mn></mrow></mfenced><mfenced><mrow><mi>x</mi><mo>+</mo><mfrac><mn>1</mn><mn>2</mn></mfrac></mrow></mfenced></math>. Multiplying both sides of this equation by <math alttext="2"><mn>2</mn>\n</math> yields <math alttext="108 equals left parenthesis 3 x plus 7 right parenthesis left parenthesis 2 x plus 1 right parenthesis"><mn>108</mn><mo>=</mo><mfenced><mrow><mn>3</mn><mi>x</mi><mo>+</mo><mn>7</mn></mrow></mfenced><mfenced><mrow><mn>2</mn><mi>x</mi><mo>+</mo><mn>1</mn></mrow></mfenced></math>. Expanding the right-hand side of this equation yields <math alttext="108 equals 6 x squared plus 3 x plus 14 x plus 7"><mn>108</mn><mo>=</mo><mn>6</mn><msup><mi>x</mi><mn>2</mn></msup><mo>+</mo><mn>3</mn><mi>x</mi><mo>+</mo><mn>14</mn><mi>x</mi><mo>+</mo><mn>7</mn></math>, or <math alttext="108 equals 6 x squared plus 17 x plus 7"><mn>108</mn><mo>=</mo><mn>6</mn><msup><mi>x</mi><mn>2</mn></msup><mo>+</mo><mn>17</mn><mi>x</mi><mo>+</mo><mn>7</mn></math>. Subtracting <math alttext="108"><mn>108</mn>\n</math> from both sides of this equation yields <math alttext="0 equals 6 x squared plus 17 x minus 101"><mn>0</mn><mo>=</mo><mn>6</mn><msup><mi>x</mi><mn>2</mn></msup><mo>+</mo><mn>17</mn><mi>x</mi><mo>-</mo><mn>101</mn></math>. It follows that the value of <math alttext="c"><mi>c</mi>\n</math> is <math alttext="negative 101"><mo>-</mo><mn>101</mn>\n</math>.</p>\n<p style="text-align: left;">Choice A is incorrect and may result from conceptual or calculation errors.</p>\n<p style="text-align: left;">Choice B is incorrect and may result from conceptual or calculation errors.</p>\n<p style="text-align: left;">Choice D is incorrect and may result from conceptual or calculation errors.</p>', 'origin': 'manifold', 'stem': '<p style="text-align: center;"><math alttext="upper A upper B equals 3 x plus 7"><mrow>\n\t<mrow>\n\t\t<mi>A</mi>\n\t\t<mi>B</mi>\n\t</mrow>\n\t<mo>=</mo>\n\t<mrow>\n\t\t<mrow>\n\t\t\t<mn>3</mn>\n\t\t\t<mi>x</mi>\n\t\t</mrow>\n\t\t<mo>+</mo>\n\t\t<mn>7</mn>\n\t</mrow>\n</mrow>\n</math></p>\n<p style="text-align: center;"><math alttext="upper B upper C equals 2 x plus 1"><mrow>\n\t<mrow>\n\t\t<mi>B</mi>\n\t\t<mi>C</mi>\n\t</mrow>\n\t<mo>=</mo>\n\t<mrow>\n\t\t<mrow>\n\t\t\t<mn>2</mn>\n\t\t\t<mi>x</mi>\n\t\t</mrow>\n\t\t<mo>+</mo>\n\t\t<mn>1</mn>\n\t</mrow>\n</mrow>\n</math></p>\n<p style="text-align: left;">In the figure, triangle <math alttext="upper A upper B upper C"><mrow>\n\t<mi>A</mi>\n\t<mi>B</mi>\n\t<mi>C</mi>\n</mrow>\n</math> is a right triangle with right angle <math alttext="upper B"><mi>B</mi>\n</math>. The area of triangle <math alttext="upper A upper B upper C"><mrow>\n\t<mi>A</mi>\n\t<mi>B</mi>\n\t<mi>C</mi>\n</mrow>\n</math> is <math alttext="54"><mn>54</mn>\n</math> square units. The lengths of sides <math alttext="upper A upper B"><mrow>\n\t<mi>A</mi>\n\t<mi>B</mi>\n</mrow>\n</math> and <math alttext="upper B upper C"><mrow>\n\t<mi>B</mi>\n\t<mi>C</mi>\n</mrow>\n</math>, in units, are given in terms of <math alttext="x"><mi>x</mi>\n</math>. The value of <math alttext="x"><mi>x</mi>\n</math> is a solution to the equation <math alttext="6 x squared plus 17 x plus c equals 0"><mrow>\n\t<mrow>\n\t\t<mn>6</mn>\n\t\t<msup>\n\t\t\t<mi>x</mi>\n\t\t\t<mn>2</mn>\n\t\t</msup>\n\t</mrow>\n\t<mo>+</mo>\n\t<mrow>\n\t\t<mn>17</mn>\n\t\t<mi>x</mi>\n\t</mrow>\n\t<mo>+</mo>\n\t<mi>c</mi>\n\t<mo>=</mo>\n\t<mn>0</mn>\n</mrow>\n</math>, where <math alttext="c"><mi>c</mi>\n</math> is a constant. What is the value of <math alttext="c"><mi>c</mi>\n</math>?</p>', 'externalid': '5b21bfe8-6b80-414f-98d1-c6d2c0ad4b6a', 'templateid': 'cbdb71d1-ff5b-45d3-bd46-6a2edff2d5a7', 'vaultid': '3e2ff0b7-8f03-4b87-9d80-c3b9c9eb1c9e', 'type': 'mcq', 'answerOptions': [{'id': '2a38f6f3-64f1-45d2-b67c-2b9cfc8f9a94', 'content': '<p><math alttext="negative 94"><mo>-</mo><mn>94</mn>\n</math></p>'}, {'id': '07e20e12-4f23-47b8-acc8-7caa172a6a7c', 'content': '<p><math alttext="negative 93"><mo>-</mo><mn>93</mn>\n</math></p>'}, {'id': '9e7f3d63-09f6-4a4a-97f7-c8a6c404a81d', 'content': '<p><math alttext="negative 101"><mo>-</mo><mn>101</mn>\n</math></p>'}, {'id': '2ef9c0c2-01b2-46c8-8990-87c9d3a6f4c6', 'content': '<p><math alttext="negative 102"><mo>-</mo><mn>102</mn>\n</math></p>'}], 'correct_answer': ['C'], 'difficulty': 'H'}



import re
from typing import Dict

try:
    from bs4 import BeautifulSoup  # type: ignore
except Exception:
    BeautifulSoup = None  # type: ignore


def _replace_math_with_alttext(soup) -> None:
    if soup is None:
        return
    for math_tag in soup.find_all("math"):
        alt_text = math_tag.get("alttext")
        replacement_text = alt_text if alt_text else math_tag.get_text(" ", strip=True)
        math_tag.replace_with(replacement_text)


def _strip_unwanted_tags(soup) -> None:
    if soup is None:
        return
    for tag_name in ["figure", "svg", "script", "style"]:
        for tag in soup.find_all(tag_name):
            tag.decompose()


def _normalize_startfraction(text: str) -> str:
    if not text:
        return text
    pattern = re.compile(r"\bStartFraction\b(.*?)\bOver\b(.*?)\bEndFraction\b", flags=re.IGNORECASE)
    while True:
        new_text, count = pattern.subn(lambda m: f"({m.group(1).strip()}) / ({m.group(2).strip()})", text)
        if count == 0:
            break
        text = new_text
    return text


def _normalize_exponent_words(text: str) -> str:
    if not text:
        return text
    # Parenthesized expression followed by 'squared' or 'cubed'
    patterns = [
        (re.compile(r"(\([^()]+\))\s*squared\b", flags=re.IGNORECASE), r"\1²"),
        (re.compile(r"(\([^()]+\))\s*cubed\b", flags=re.IGNORECASE), r"\1³"),
        # Simple token (variable/number) followed by 'squared' or 'cubed'
        (re.compile(r"\b([A-Za-z0-9]+)\s*squared\b", flags=re.IGNORECASE), r"\1²"),
        (re.compile(r"\b([A-Za-z0-9]+)\s*cubed\b", flags=re.IGNORECASE), r"\1³"),
    ]
    changed = True
    while changed:
        changed = False
        for pat, repl in patterns:
            new_text, count = pat.subn(repl, text)
            if count:
                changed = True
                text = new_text
    return text


def _apply_word_replacements(text: str) -> str:
    replacements = [
        (r"\bleft parenthesis\b", "("),
        (r"\bright parenthesis\b", ")"),
        (r"\bcomma\b", ","),
        (r"\bStartRoot\b", "√("),
        (r"\bEndRoot\b", ")"),
        (r"\bupper\s+([A-Za-z])\b", r"\1"),
        (r"\bequals\b", "="),
        (r"\bpi\b", "π"),
        (r"\bminus\b", "−"),
        (r"\bnegative\b", "−"),
        (r"\bplus\b", "+"),
    ]
    normalized = text
    for pattern, repl in replacements:
        normalized = re.sub(pattern, repl, normalized, flags=re.IGNORECASE)
    return normalized


def _normalize_math_words_to_symbols(text: str) -> str:
    if not text:
        return text

    # Fractions first
    text = _normalize_startfraction(text)

    # First-pass token cleanup (parenthesis words, etc.) before exponent handling
    text = _apply_word_replacements(text)

    # Handle combined operators before single-word replacements
    text = re.sub(r"\bplus\s+or\s+minus\b", "±", text, flags=re.IGNORECASE)
    text = re.sub(r"\bplus\s+or\s+[\-−]\b", "±", text, flags=re.IGNORECASE)

    # Exponent words (squared/cubed)
    text = _normalize_exponent_words(text)

    # Second pass to eliminate any tokens introduced near exponents
    text = _apply_word_replacements(text)

    # Clean spaces around punctuation, operators, and parentheses
    text = re.sub(r"\s+\)", ")", text)
    text = re.sub(r"\(\s+", "(", text)
    text = re.sub(r"\s+,\s*", ", ", text)
    text = re.sub(r"\s+([+−=×/])\s+", r" \1 ", text)

    # Collapse any leftover excessive whitespace
    text = re.sub(r"\s+", " ", text).strip()
    return text


def html_to_plain_text(html_str: str) -> str:
    if not html_str:
        return ""

    # Preferred path: BeautifulSoup for robust parsing
    if BeautifulSoup is not None:
        soup = BeautifulSoup(html_str, "html.parser")
        _strip_unwanted_tags(soup)
        _replace_math_with_alttext(soup)
        plain_text = soup.get_text(" ", strip=True)
    else:
        # Fallback: regex-based stripping with special handling for MathML alttext
        tmp = html_str
        # Replace math blocks with their alttext when present
        tmp = re.sub(r'<math[^>]*alttext="([^"]+)"[^>]*>.*?</math>', r'\1', tmp, flags=re.DOTALL | re.IGNORECASE)
        # Drop script/style/svg/figure contents entirely
        tmp = re.sub(r'<(script|style|svg|figure)\b[^>]*>.*?</\1>', ' ', tmp, flags=re.DOTALL | re.IGNORECASE)
        # Strip remaining tags
        tmp = re.sub(r'<[^>]+>', ' ', tmp)
        # Collapse whitespace
        plain_text = re.sub(r'\s+', ' ', tmp).strip()

    try:
        from html import unescape
        plain_text = unescape(plain_text)
    except Exception:
        pass

    # Normalize verbose MathML alttext wording to readable symbols
    plain_text = _normalize_math_words_to_symbols(plain_text)

    return plain_text


def build_readable_text(item: Dict) -> str:
    parts = []

    origin = item.get("origin")
    if origin:
        parts.append(f"Origin: {origin}")

    stem_html = item.get("stem")
    if stem_html:
        parts.append("Question:")
        parts.append(html_to_plain_text(stem_html))

    answer_options = item.get("answerOptions") or []
    if answer_options:
        parts.append("Choices:")
        letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
        for idx, option in enumerate(answer_options):
            label = letters[idx] if idx < len(letters) else f"Option {idx+1}"
            content_html = option.get("content", "")
            content_text = html_to_plain_text(content_html)
            parts.append(f"{label}) {content_text}")

    correct_letters = item.get("correct_answer") or []
    numeric_keys = item.get("keys") or []

    if correct_letters:
        parts.append(f"Correct: {', '.join(correct_letters)}")
    elif numeric_keys:
        letters = []
        for key in numeric_keys:
            try:
                index_one_based = int(key)
                letters.append("ABCDEFGHIJKLMNOPQRSTUVWXYZ"[index_one_based - 1])
            except Exception:
                letters.append(str(key))
        parts.append(f"Correct: {', '.join(letters)}")

    rationale_html = item.get("rationale")
    if rationale_html:
        parts.append("Explanation:")
        parts.append(html_to_plain_text(rationale_html))

    difficulty = item.get("difficulty")
    if difficulty:
        parts.append(f"Difficulty: {difficulty}")

    return "\n".join(parts)


if __name__ == "__main__":
    print(build_readable_text(text))
